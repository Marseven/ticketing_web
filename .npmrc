node-linker=hoisted
# Ne pas relancer `pnpm install` avant chaque script (esbuild fonctionne via le
# store .pnpm ; le check bloquait `pnpm build` sur l'approbation de builds).
verify-deps-before-run=false
